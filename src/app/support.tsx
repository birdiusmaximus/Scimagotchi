import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function SupportScreen() {
  return (
    <PlaceholderScreen
      title="Support"
      subtitle="Ways to find help when you need it. This is a reflection companion, not a crisis service."
    />
  );
}
